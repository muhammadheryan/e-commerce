# App
SERVER_PORT=8080
ENV=development
PROJECT_NAME=ecommerce_local_docker

# Database
DB_HOST=mysql-ecommerce
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=e-commerce

# Database connection URL
DATABASE_URL=mysql://root:root@tcp(mysql-ecommerce:3306)/e-commerce?parseTime=true

# DB connection pool
DB_MAX_OPEN_CONNS=10
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=3600

# Server timeouts (seconds)
SERVER_READ_TIMEOUT=5
SERVER_WRITE_TIMEOUT=10
SERVER_IDLE_TIMEOUT=30

# Redis (docker service name)
REDIS_HOST=redis-ecommerce
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT & Auth
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRATION=86400
SESSION_EXPIRATION=86400

# Internal API key for internal-only routes (MQ consumer)
INTERNAL_API_KEY=xyz-test-only

# Order expiration (seconds)
ORDER_EXPIRES_SECONDS=3600

# RabbitMQ (docker service name)
RABBITMQ_HOST=rabbitmq-ecommerce
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
